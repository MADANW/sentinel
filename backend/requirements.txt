# Pin exact versions for reproducibility and supply chain security.
# After changing versions, run: pip-audit -r requirements.txt
# To update a package: pip install --upgrade <package> && pip freeze > requirements.txt

# Trading
alpaca-py==0.25.0

# AI
anthropic==0.26.0

# Data / signals
pandas==2.2.1
numpy==1.26.4
# pandas-ta removed: 0.3.14b0 pre-release is incompatible with pandas>=2.0 and
# doesn't resolve on PyPI. EMA crossover (Sprint 2) will add a working TA library.

# Environment
python-dotenv==1.0.1

# Logging / observability
structlog==24.1.0

# Supabase
supabase==2.4.3

# HTTP (used by alpaca-py and anthropic)
httpx==0.27.0

# Testing
pytest==8.1.1
pytest-asyncio==0.23.6
