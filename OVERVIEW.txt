Sentinel — Full Project Overview
=================================

WHAT IT DOES
------------
You provide morning news headlines. The bot queries Claude for a directional
bias, validates it, checks risk limits, and submits a bracket order to Alpaca.
Everything is logged to Supabase and visible in the dashboard.


ARCHITECTURE
------------
Headlines (you)
     |
     v
morning_pipeline.py  ->  Claude API  ->  parse_bias_response()
     |
     v
TradingBias (bullish/bearish, confidence >= 60%)
     |
     v
risk_engine.py  ->  check_kill_switch()  ->  validate_order()  ->  qty
     |
     v
order_executor.py  ->  Alpaca bracket order (entry + stop + target)
     |
     v
journal.py  ->  Supabase trades table
     |
     v
dashboard/  ->  Next.js page (P&L, trade table, kill switch status)


FILES
-----
sentinel/
├── backend/core/
│   ├── risk_engine.py        — Hardcoded limits + kill switch
│   ├── bias_validator.py     — Claude response sanitizer
│   ├── signals.py            — EMA crossover (pandas.ewm)
│   ├── morning_pipeline.py   — Claude API orchestration
│   ├── order_executor.py     — Alpaca bracket orders
│   └── journal.py            — Supabase trade logging
├── backend/tests/
│   ├── test_risk_engine.py   — 22 tests
│   ├── test_smoke.py         — 20 tests (Sprint 2 E2E)
│   └── test_journal.py       — 11 tests
├── dashboard/
│   ├── app/page.tsx          — P&L summary + trade table
│   ├── app/layout.tsx
│   └── lib/supabase.ts       — Server-side Supabase client
├── supabase/migrations/
│   └── 001_create_trades.sql — trades table schema
└── .github/workflows/
    └── security.yml          — gitleaks, pip-audit, bandit, pytest, npm-audit


RISK RULES (hardcoded, no overrides)
-------------------------------------
Risk per trade      1% of equity
Daily loss limit    2%  ->  kill switch  ->  sys.exit(1)
Max trades/day      3
Min confidence      60%


TEST COVERAGE
-------------
Sprint 1    22 tests   risk engine + bias validator
Sprint 2    20 tests   signals, pipeline, order executor, E2E
Sprint 3    11 tests   journal
Sprint F    17 tests   Hurst, OU, regime features
Other       69 tests   feature engineering, model, monte carlo, morning pipeline
Total       139 passing


ML PIPELINE (Sprint F)
----------------------
XGBoost classifier trained on SPY + QQQ + IWM (756 days, per-ticker
chronological split then concat). 12 features: 8 legacy momentum/volume
indicators plus 4 regime features — Hurst exponent, log(1 + OU half-life),
OU z-score, and a derived regime_label (trend / random walk / range).

Retrain:
  python -m backend.scripts.train_model --tickers SPY,QQQ,IWM --days 756

Current model: AUC-ROC 0.5623 (above 0.55 minimum gate).


WHAT'S LEFT
-----------
- Sprint E: paper-trading loop / CODEOWNERS (deferred)
- MT5 bridge in mql5/ (deferred)
- Schedule the morning pipeline (cron / systemd timer)
